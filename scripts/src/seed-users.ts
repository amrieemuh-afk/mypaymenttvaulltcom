/**
 * Reference: to seed users, run the seed script in the db package:
 *   pnpm --filter @workspace/db run seed
 *
 * This seeds admin/hr/finance accounts with bcrypt-hashed passwords
 * and registered email addresses into the users table.
 *
 * Credentials: admin/admin123, hr/hr123, finance/finance123
 */

console.log("Run: pnpm --filter @workspace/db run seed");
